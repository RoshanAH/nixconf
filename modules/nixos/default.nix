{
  # No custom NixOS modules yet. When you add one (e.g. ./my-foo.nix), export it
  # here and it will be auto-imported by hosts/common/global/default.nix.
  #
  # Example:
  #   my-foo = import ./my-foo.nix;
}
